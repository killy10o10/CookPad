<%= form_with(model: inventory_food, url: inventory_inventory_foods_path) do |form| %>
  <% if inventory_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(inventory_food.errors.count, "error") %> prohibited this inventory_food from being saved:</h2>

      <ul>
        <% inventory_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :Please_select_a_food_item_to_add, class: "d-block"%>
    <%= form.collection_select :food_id, all_foods, :id, :name, :prompt => true %>
   </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
